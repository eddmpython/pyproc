// config.js - written by the installer: the native messaging host this extension talks to. A preset pairing digest
// exists only in the isolated browsers of pyproc's own gates, which cannot click the extension action.
export const HOST_NAME = "com.pyproc.user_browser";
export const PRESET_PAIRING_SHA256 = null;
