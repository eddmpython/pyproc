# 내부 에이전트/개발 규칙 문서·설정은 로컬 전용(공개 레포/패키지에 올리지 않음). codaro/dartlab 동급.
CLAUDE.md
AGENTS.md
.claude/

node_modules/
npm-debug.log*
*.tgz
.DS_Store
Thumbs.db
.cache/
.vscode/
.idea/
scratch/
*.local

# 실측용 외부 자산(레포 자산 0 유지. 재현 레시피는 해당 캠페인 README):
# 대형 엔진 바이너리 + vendored 서드파티 shim(라이선스 고지 부담도 회피).
tests/attempts/enginePort/*.wasm
tests/attempts/enginePort/browserWasiShim.js
